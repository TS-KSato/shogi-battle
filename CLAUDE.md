# CLAUDE.md

このファイルは Claude Code への指示書です。作業前に必ず読んでください。

---

## このリポジトリは何か

「鬼謀」。9×9の戦場で兵を動かし、敵の大将を追い詰めるスマートフォン向けの
一人用3Dボードゲーム。GitHub Pages で静的配信します。

**核となる体験**：無機質な駒ではなく、生きている兵として戦場に見せる。
プレイヤーの立ち位置は指し手ではなく、兵を率いる軍師。

**ルール層の土台は将棋**です。土台に選んだのは、ルールの設計と検証の工程を
短縮するため（perft で既知の値と照合できる）。この文書のような開発者向けの
文書には土台を明記しますが、**プレイヤー向けの画面と文書では将棋として
紹介しません**。画面の文言は将棋の用語を知らなくても読めるようにします
（後述「画面の用語」）。

---

## 構成

```
index.html               タイトル。全画面の背景絵、ロゴ、順番と強さの選択、
                         ガイドの入口。開いた時点で兵の GLB を先読みする
play3d.html              対局画面（3D）。UI・描画・演出をこの1ファイルに持つ
guide.html               ガイド。兵・ルール・操作・困ったときの4区分。
                         対局画面からは「？」で iframe に重ねて開く
src/name.js              ゲーム名。index・play3d・guide のタブ名と見出しで共有
src/shogi.js             ルール層   盤面・合法手・終局判定・千日手
src/engine.js            思考層     NPC の探索と評価
src/npc.js               接続層     本体から Worker を呼ぶ窓口。退避処理を持つ
src/npc.worker.js        接続層     Worker 側。engine.js を動かす
assets/*.glb             3Dモデル25個（後述「3Dアセット」）
assets/title.jpg         タイトルの背景絵（任意。無ければ中央寄せの配置に戻る）
assets/logo.png          タイトルのロゴ（任意。無ければ文字の見出し）
assets/stage/honjin.jpg  舞台絵。対局画面の背景
assets/guide/*.png       ガイドの兵の絵10枚。tools/guide-icons.mjs で GLB から生成
assets/guide/rule-*.jpg  ガイドのルールの実写6枚。play3d.html?sfen= で撮る
test/                    ルール層と NPC のテスト
tools/guide-icons.mjs    ガイドの兵の絵の生成（配信しない）
```

---

## 絶対に守ること

### 1. 層の境界を越えない

- **`src/shogi.js` は、DOM・Three.js・演出・Worker を一切 import しない。**
  Node 上で単体で動くこと。この性質が壊れたら、テストが実行できなくなります。
- **`src/engine.js` は `src/shogi.js` だけに依存する。**
- 表現層（play3d.html）は、ルール層の結果を受け取って再生するだけ。
  **演出側で合法性や勝敗を判定しない。**

「動かすため」に表現層からルール層を直接書き換えるのは、いかなる理由でも禁止です。

### 2. ルールを変えない

同じ局面から得られる合法手と勝敗は、通常の将棋と完全に一致させます。
HP、命中率、乱数による成否、駒ごとの特殊能力は導入しません。
演出は結果の見せ方であって、結果そのものを変えません。

### 3. 変更したらテストを走らせる

```bash
node test/shogi.test.mjs          # ルール層（数秒）
node test/shogi.test.mjs --full   # perft 深さ5まで（約40秒）
node test/engine.test.mjs         # NPC（約1分）
node test/engine.test.mjs --games # 自己対局で難易度の順序も見る（数分）
```

`src/shogi.js` に触ったら、`--full` まで必ず通してください。
報告には両方の出力を貼ってください。

`engine.test.mjs` の「easy 同士が終局する」は乱数を含む自己対局なので、
手数上限300に達して時々落ちます。落ちたら再実行して判断してください。

**perft の受け入れ値**（初期局面、公開されている検証済みの値）

| 深さ | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 局面数 | 30 | 900 | 25,470 | 719,731 | 19,861,490 |

最多合法手局面 `R8/2K1S1SSk/4B4/9/9/9/9/9/1L1L1L3 b RBGSNLP3g3n17p 1`
は 深さ1 = 593、深さ2 = 105,677、深さ3 = 53,393,368。

**ここが1つでも合わなければ、合法手生成のどこかが必ず壊れています。**
数字を書き換えて通すことは絶対にしないでください。

### 4. 絶対パスを使わない

GitHub Pages は `https://<user>.github.io/<repo>/` というサブパスで配信されます。
`/src/shogi.js` のような絶対パスは全て 404 になります。
必ず `./src/shogi.js` `../src/shogi.js` の形で書いてください。

### 5. ブラウザストレージを使わない

対局の途中保存も棋譜保存もしません。状態はメモリ上に持ちます。

---

## 表記の約束

- 局面は **SFEN**、指し手は **USI** 形式。独自形式を作らない
  - 局面 `lnsgkgsnl/1r5b1/ppppppppp/9/9/9/PPPPPPPPP/1B5R1/LNSGKGSNL b - 1`
  - 移動 `7g7f` / 成り `8h2b+` / 打つ `P*5e`
- 駒の内部名 `fu / kyo / kei / gin / kin / kaku / hisha / gyoku`、
  成りは `to / narikyo / narikei / narigin / uma / ryu`
- 盤の座標は 1マス = 1.0 単位。モデルの原点は足元中央、正面は +Z、上方向は +Y
- 陣営は `blue`（自軍・画面手前）と `red`（敵軍・画面奥）

---

## 画面の用語

将棋の用語をそのまま出さず、兵の姿に合わせた呼び名を使います。
新しい文言を足すときもこの語彙に揃えてください。

| 将棋 | 画面 |
|---|---|
| 盤 | 戦場 |
| 駒 | 兵 |
| 自分・相手 | 自軍・敵軍 |
| 先手・後手 | 先攻・後攻 |
| 手番 | 「自軍の行動」／「敵軍の行動」 |
| 成る | 進化する |
| 成り駒 | 「歩兵（進化）」のように元の名に付ける |
| 王手 | 戦闘ログは「大将に迫る」、行動の表示は「・大将が危ない」 |
| 詰み | 「敵の大将を追い詰めた」／「大将を追い詰められた」 |
| 手詰まり | 「敵軍に動ける兵がない」／「自軍に動ける兵がない」 |
| 持ち駒 | 控え（画面下は控え欄） |
| 打つ | 投入する |
| 棋譜 | 戦闘ログ（「自軍の歩兵が7六へ進む」のような文） |
| 千日手 | 「同じ配置が4回くり返されたため」 |
| 千日手（連続王手） | 「大将に迫り続けたため、あなたの負け」 |
| 投了 | 降参 |
| 対局 | 対戦（「新しい対戦」）。始めるのは「出陣する」 |

兵の呼び名：歩兵（歩）、槍兵（槍）、忍者（忍）、侍（侍）、近衛兵（衛）、
僧兵（僧）、鉄砲兵（砲）、大将。括弧の一文字は控え欄に出る印。

**画面に将棋の用語を足さないこと。**

---

## 対局の仕様

- 人間 対 NPC。手番と強さはタイトル画面から `?side=` `?level=` で渡す。
  無い・不正なら先手・ふつう
  - 順番：先攻 = `0`、後攻 = `1`。どちらでも自軍は画面手前（後攻では戦場を反転する）
  - 強さ：やさしい `easy`、ふつう `normal`、つよい `hard`
- 終局は、詰み・手詰まり（指せる手がない）・投了・千日手
  - 千日手：同一局面が4回目に現れたら引き分け。一方が王手を続けていた場合は
    その側の負け。判定は `src/shogi.js` の `repetition`、履歴の記録と終局処理は
    対局側
  - 持将棋は実装しない
- NPC が時間内に手を返せないときは、接続層（`src/npc.js`）が合法手からランダムに
  1手選ぶ（退避手）。対局は止めない。対局側は返ってきた手の合法性を確かめ、
  非合法なら同じく合法手から1手選ぶ。退避手が2手続いたときだけ画面に知らせる
- 王手中に、動かせる手のない兵を選ぶと「この兵では大将を守れません」と出す

---

## 設計判断と根拠

実機で比較して決めた事項です。変える場合は根拠ごと覆してください。

| 決定 | 根拠 |
|---|---|
| **待機モーションは骨で再生する** | 頂点シェーダの揺れでは駒種ごとに動きを変えられない。骨付きなら駒種ごとの待機を持てる |
| **移動に歩行サイクルを付けない** | 1マス0.35秒では歩数が知覚されない。弧を描いて滑らせる |
| **撃破は「つぶれて消える」** | 「弾かれて飛ぶ」「崩れて沈む」と比較して選択。現実の死に方を参照しない |
| **NPCは自作の単スレッド探索** | GitHub Pages は COOP/COEP ヘッダを設定できず、SharedArrayBuffer が使えない。マルチスレッドWASMエンジンは動かない |
| **強さは思考時間で決める** | 探索深さで決めると、端末の速さがそのまま強さの差になる |
| **キャラクターは駒種ごとのプールから個別に描く** | 骨の姿勢は個体ごとに違うのでインスタンシングにできない。駒種ごとにモデルを分けるため、プールも駒種ごとに持つ |
| **インスタンシングは退避経路だけ** | 骨付きGLBが読めないときは骨なしGLB→箱の素体へ落ち、そこでは1陣営1回の描画に戻す |
| **兵の表示倍率は 1.3** | 実機で見比べて決めた値。足元を原点に一様拡大するので足元は動かない |
| **ズームはピンチのみ。ボタンは「ズーム解除」1つ** | スライダーはピンチと重複する。ボタンは拡大中だけ表示する |
| **成り駒のうち金と同じ動きになる5種は、元の姿のまま幟を立てる** | 幟は「金の動きをする駒」の印。馬・龍は動きが違うので専用モデル。龍のマスにはさらに大砲を置く |

---

## 3Dアセット

`assets/` の GLB は、すべてスキンと待機アニメを1本持ちます（幟と大砲は静止形状、
骨なしの退避用は待機アニメなし）。

**ファイル名**

| 用途 | 名前 |
|---|---|
| 通常の駒 | `<内部名>_<blue|red>_idle.glb` |
| 馬・龍 | `kaku_<blue|red>_promoted.glb` `hisha_<blue|red>_promoted.glb` |
| 幟 | `nobori_blue.glb` `nobori_red.glb` |
| 大砲（龍のマスに置く） | `taiho.glb` |
| 骨なしの退避用（GLTFLoader が読めないとき） | `fu_blue.glb` `fu_red.glb` |

読み込みは駒種ごとにこのファイルを試し、無ければ元の駒→歩の順で代用します。
ファイルを足すだけで反映されます。

**受け入れ条件**

| 項目 | 値 |
|---|---|
| 全高 | 0.7026（武器・幟・立物は超えてよい） |
| 足元 | y = 0 |
| 正面 | +Z |
| 三角形 | 本体600（部品を足す場合は加算） |
| テクスチャ | 256px JPEG |
| アニメーション | 1本のみ |

アニメーションが2本以上入っていると、Three.js が `animations[0]` を再生し、
意図しない動きになります。

**識別の原則**

- 色は陣営の軸、形は駒種の軸。両方を混ぜない
- 盤上の実寸（約46px）で見分けられることを基準にする。静止画の見栄えではない
- 俯角35度なので読めるのは頭部の輪郭。頭部を明るい陣営色にする
- 白は角（僧兵）の識別色。他の駒で白を増やすときは競合を確認する

---

## 作業の進め方

- **小さく変えて、すぐコミットする。** 動いた時点で必ずコミットしてください
- 大きな書き換えを提案する前に、テストが通る状態を保存してから始める
- 実装できない・仕様が矛盾していると気づいたら、勝手に回避せず報告してください
- 作業は main ブランチで行い、main に直接 push する。ブランチと pull request は作らない
- 終わったらファイル数と変更行数を報告する。妥当な規模は1ファイル・数十行
- 実装が1つしかない抽象化を作らない
- 起きていない失敗への対処を書かない
- 設定を外部ファイルに出さない。定数はコードに直接書く
- コメントは「なぜ」だけ書く。コードを読めば分かることを日本語で言い直さない
- 依頼されていないファイルや機能を追加しない
- 表示の変更は、実機の幅（375〜440px）で崩れないことを確認してから報告する

---

## 実装していないもの

- **持将棋**。実装しない方針
- **詰将棋モード**。問題データは著作物なので、既存の本やサイトから転載できない。
  作るなら自動生成とソルバー検証が必要
- **成り・王手・詰みの演出**。駒取りの演出のみ実装済み
- iOS Safari での Worker の動作は未検証

3Dモデル・音・ライブラリの利用条件は、公開前に一次情報で確認すること。
