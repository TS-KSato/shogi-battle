# CLAUDE.md

このファイルは Claude Code への指示書です。作業前に必ず読んでください。

---

## このリポジトリは何か

将棋のルールをそのまま使い、駒を和風デフォルメ3Dキャラクターとして表示する、
スマートフォン向けの一人用ゲーム。GitHub Pages で静的配信します。

**核となる体験**：無機質な駒ではなく、生きている兵として盤上に見せることで、
将棋への入口を下げる。操作も画面構成も普通の将棋ゲームと変わらないが、
体感として棋士ではなく軍師の気分になる、という程度を狙う。

---

## 絶対に守ること

### 1. 層の境界を越えない

```
ルール層   src/shogi.js          盤面・合法手・終局判定
思考層     src/engine.js         NPC の探索と評価
接続層     src/npc.js, npc.worker.js
表現層     3D描画・演出・アニメーション
UI層       入力・合法手表示・メニュー
```

- **`src/shogi.js` は、DOM・Three.js・演出・Worker を一切 import しない。**
  Node 上で単体で動くこと。この性質が壊れたら、テストが実行できなくなります。
- **`src/engine.js` は `src/shogi.js` だけに依存する。**
- 表現層は、ルール層の結果を受け取って再生するだけ。
  **演出側で合法性や勝敗を判定しない。**

「動かすため」に表現層からルール層を直接書き換えるのは、いかなる理由でも禁止です。

### 2. ルールを変えない

同じ局面から得られる合法手と勝敗は、通常の将棋と完全に一致させます。
HP、命中率、乱数による成否、駒ごとの特殊能力は導入しません。
演出は結果の見せ方であって、結果そのものを変えません。

### 3. 変更したらテストを走らせる

```bash
node test/shogi.test.mjs          # ルール層（数秒）
node test/shogi.test.mjs --full   # perft 深さ5まで（約40秒）
node test/engine.test.mjs         # NPC（約1分）
```

`src/shogi.js` に触ったら、`--full` まで必ず通してください。

**perft の受け入れ値**（初期局面、公開されている検証済みの値）

| 深さ | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 局面数 | 30 | 900 | 25,470 | 719,731 | 19,861,490 |

最多合法手局面 `R8/2K1S1SSk/4B4/9/9/9/9/9/1L1L1L3 b RBGSNLP3g3n17p 1`
は 深さ1 = 593、深さ2 = 105,677、深さ3 = 53,393,368。

**ここが1つでも合わなければ、合法手生成のどこかが必ず壊れています。**
数字を書き換えて通すことは絶対にしないでください。

### 4. 絶対パスを使わない

GitHub Pages は `https://<user>.github.io/<repo>/` というサブパスで配信されます。
`/src/shogi.js` のような絶対パスは全て 404 になります。
必ず `./src/shogi.js` `../src/shogi.js` の形で書いてください。

### 5. ブラウザストレージを前提にしない

初期版では対局の途中保存も棋譜保存も必須ではありません。
状態はメモリ上に持ちます。

---

## 表記の約束

- 局面は **SFEN**、指し手は **USI** 形式。独自形式を作らない
  - 局面 `lnsgkgsnl/1r5b1/ppppppppp/9/9/9/PPPPPPPPP/1B5R1/LNSGKGSNL b - 1`
  - 移動 `7g7f` / 成り `8h2b+` / 打つ `P*5e`
- 駒の内部名 `fu / kyo / kei / gin / kin / kaku / hisha / gyoku`、成りは `to / narikyo / narikei / narigin / uma / ryu`
- 盤の座標は 1マス = 1.0 単位。モデルの原点は足元中央、正面は +Z、上方向は +Y

---

## 検証済みの設計判断（覆さないこと）

実機で比較して決めた事項です。理由なく元に戻さないでください。

| 決定 | 根拠 |
|---|---|
| **待機モーションは骨で再生する** | 頂点シェーダの揺れでは駒種ごとに動きを変えられない。骨付きなら駒種ごとの待機を持てる |
| **移動に歩行サイクルを付けない** | 1マス0.35秒では歩数が知覚されない。弧を描いて滑らせる |
| **撃破は「つぶれて消える」** | 「弾かれて飛ぶ」「崩れて沈む」と比較して選択。現実の死に方を参照しない |
| **NPCは自作の単スレッド探索** | GitHub Pages は COOP/COEP ヘッダを設定できず、SharedArrayBuffer が使えない。マルチスレッドWASMエンジンは動かない |
| **強さは思考時間で決める** | 探索深さで決めると、端末の速さがそのまま強さの差になる |
| **キャラクターは駒種ごとのプールから個別に描く** | 骨の姿勢は個体ごとに違うのでインスタンシングにできない。駒種ごとにモデルを分けるため、プールも駒種ごとに持つ |
| **インスタンシングは退避経路だけ** | 骨付きGLBが読めないときは骨なしGLB→箱の素体へ落ち、そこでは1陣営1回の描画に戻す |

その結果、**リグとアニメーションクリップが必要**です。
3Dアセットはスキンと待機アニメを持つ GLB を扱います（幟・大砲だけは静止形状）。

---

## 進行状況

- [x] ルール層（perft 一致、禁じ手テスト 31件）
- [x] NPC（戦術・合法性・時間のテスト 9件）
- [x] 待機モーションの実機比較 → `lab/idle-test.html`
- [x] 駒取り演出の実機比較 → `lab/capture-test.html`
- [x] 2D盤面で最後まで対局できる1ページ → `play.html`（人間 対 NPC）
- [x] `npc.worker.js` をブラウザ上で動作確認（`play.html` で worker 応答を確認）
- [x] 3D表現の統合（駒種ごとの骨付きGLB・待機モーション・移動・駒取り演出）
- [x] 成り駒の表現（金の動きをする5種は幟、馬・龍は専用GLB、龍は大砲）
- [ ] **詰将棋モード** ← 次はここ

---

## 作業の進め方

- **小さく変えて、すぐコミットする。** 動いた時点で必ずコミットしてください
- 大きな書き換えを提案する前に、テストが通る状態を保存してから始める
- 実装できない・仕様が矛盾していると気づいたら、勝手に回避せず報告してください
- 作業は main ブランチで行い、main に直接 push する。ブランチと pull request は作らない
- 実装が1つしかない抽象化を作らない
- 起きていない失敗への対処を書かない
- 設定を外部ファイルに出さない。定数はコードに直接書く
- コメントは「なぜ」だけ書く。コードを読めば分かることを日本語で言い直さない
- 依頼されていないファイルや機能を追加しない

---

## 未解決の課題

- **千日手・持将棋の規則が未実装。** NPC側でくり返しを避けているだけで、正式な規則ではありません
- **`npc.worker.js` / `npc.js` の実機確認は `play.html` での通し対局のみ。** module Worker の起動・思考応答・退避処理は確認済みだが、端末差（特に iOS Safari）の検証はこれから
- 詰将棋の問題データは著作物です。既存の本やサイトから転載できません
- 3Dモデル・音・ライブラリの利用条件は、公開前に一次情報で確認すること
